package util

import com.tzavellas.sse.guice.ScalaModule

class InjectorModule extends ScalaModule {
  def configure() = {
//    bind[UserRepository].to[UserRepository]
//    bind[SessionRepository].to[SessionRepository]
//    bind[ContactRepository].to[ContactRepository]
  }
}